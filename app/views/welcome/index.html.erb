<h1> Hello XSN </h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Location</th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.idea.title %></td>	
        <td><%= event.location %></td>
      </tr>
    <% end %>
  </tbody>
</table>
