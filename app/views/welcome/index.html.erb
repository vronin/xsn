<table>
  <tbody>
	<tr>
		Here are couple of samples on things to do. Sign in to see more.
		
    	<% @random_events.each do |event| %>
			<td>
				<%= image_tag event.image.url(:medium) %><br>
        		<%= event.title %> at 
       			<%= event.location %>
			</td>
    	<% end %>
   </tr>
  </tbody>
</table>
