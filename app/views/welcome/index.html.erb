<table>
  <tbody>
    <% @events.each do |event| %>
      <tr>
		<td><%= image_tag event.image.url(:medium) %></td>
        <td><%= event.title %></td>	
        <td><%= event.location %></td>
      </tr>
    <% end %>
  </tbody>
</table>
